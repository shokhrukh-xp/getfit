#!/bin/sh
# Все проверки. С 24.09 идут параллельно (tg/run-fast.js) — около минуты
# вместо двадцати. Сайт прогон поднимает сам, site/ и srv.sh ему не нужны.
#   sh tg/run.sh              все наборы, обходчик и тыкалка
#   sh tg/run.sh eda priem    только наборы с «eda» или «priem» в имени
#   sh tg/run.sh -j 1         по одному, как раньше
#   sh tg/run.sh --все        обходчик на всех четырёх размерах экрана
#   GF_SLOW=1 sh tg/run.sh    старые слепые паузы после загрузки страницы
cd "$(dirname "$0")/.." || exit 1
exec node tg/run-fast.js "$@"
